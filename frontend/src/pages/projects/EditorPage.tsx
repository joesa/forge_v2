export default function EditorPage() {
  return (
    <div style={{ height: '100vh', background: 'var(--surface)', display: 'flex', alignItems: 'center', justifyContent: 'center' }}>
      <h1 style={{ fontSize: 24, fontWeight: 800, color: 'var(--text)' }}>Editor — Full Viewport</h1>
    </div>
  )
}
